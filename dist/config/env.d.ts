export const __esModule: boolean;
export namespace env {
    let NODE_ENV: string;
    let PORT: number;
    let MONGO_URI: string;
    let JWT_ACCESS_SECRET: string;
    let JWT_REFRESH_SECRET: string;
    let JWT_ACCESS_EXPIRES: string;
    let JWT_REFRESH_EXPIRES: string;
    let SENDGRID_API_KEY: string;
    let EMAIL_FROM: string;
    let CORS_ORIGIN: string;
    let FILE_STORAGE: string;
    let UPLOAD_DIR: string;
    let BASE_URL: string;
    let ENABLE_2FA: boolean;
}
